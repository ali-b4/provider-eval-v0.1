# Provider Evaluation

Evaluation harness for comparing inference providers.

## Structure

- `providers/` — provider adapters
- `tests/` — benchmark test cases
- `scripts/` — request and benchmark runners
- `results/` — raw outputs and summaries
- `config/` — pricing and evaluation configuration

